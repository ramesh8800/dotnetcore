apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: dotnetcore-dep
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: jenkins
    spec:
      containers:
        - name: jenkins
          image: ramesh8800/dotnetcore:$BUILD_NUMBER
          imagePullPolicy: IfNotPresent
          ports:
            - name: http-port
              containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: dotnetcore-svc
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: jenkins
